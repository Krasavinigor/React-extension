export enum EFooterState {
  HasMutualRecruiters,
  NoMutualRecruiters,
  WrongPage,
  Loading,
  Error,
}

export enum EReactionMessage {
  Auth = "Auth",
  NoTemplates = "NoTemplates",
  ErrorState = "ErrorState"
}
